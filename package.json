{
  "license": "",
  "type": "module",
  "scripts": {
    "start": "vite",
    "build": "run-s clean build-only",
    "build-only": "tsc && vite build",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "pixi.js": "^8.0.0"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "rimraf": "^5.0.5",
    "typescript": "^5.2.2",
    "vite": "^5.1.4"
  }
}