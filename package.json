{
  "license": "",
  "source": "src/index.html",
  "scripts": {
    "start": "parcel serve --open",
    "build": "run-s clean build-only",
    "build-only": "parcel build --public-url .",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "pixi.js": "^6.3.0"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "parcel": "^2.7.0",
    "querystring-es3": "^0.2.1",
    "rimraf": "^3.0.2",
    "typescript": "^4.6.3"
  }
}
